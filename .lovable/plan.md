## Update navigation labels to "Stages"

Update `src/components/StickyNav.tsx`:
- Corporate links: `"The Journey"` → `"The Stages"`
- Individual links: `"Your Journey"` → `"Your Stages"`

The hash anchor (`#journey`) stays the same so scroll-to-section keeps working.